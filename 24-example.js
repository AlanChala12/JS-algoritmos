// Construir una función que reciba como parámetro un entero y retorne 1 si dicho valor es múltiplo de 5. Deberá retornar 0 si no es así.

function mulOfFive( int ) {
   return int % 5 == 0 ? 1 : 0;
}
 
mulOfFive(5);
 